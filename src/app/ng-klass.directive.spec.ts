import { NgKlassDirective } from './ng-klass.directive';

describe('NgKlassDirective', () => {
  it('should create an instance', () => {
    const directive = new NgKlassDirective();
    expect(directive).toBeTruthy();
  });
});
